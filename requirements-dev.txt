# Test dependencies. Install on top of the runtime requirements:
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0

# Only needed to rebuild assets/before-after.png (src/build_banner.py),
# not to run the test suite.
pymupdf>=1.24
pillow>=10.0
